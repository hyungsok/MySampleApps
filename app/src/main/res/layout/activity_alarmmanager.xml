<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.onlyapps.sample.AlarmManagerTestActivity">

    <Button
        android:id="@+id/schedule_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="40dp"
        android:layout_marginRight="40dp"
        android:layout_marginTop="20dp"
        android:onClick="scheduleJob"
        android:text="Schedule Job" />

    <Button
        android:id="@+id/cancel_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/schedule_button"
        android:layout_alignLeft="@+id/schedule_button"
        android:layout_alignRight="@+id/schedule_button"
        android:layout_alignStart="@+id/schedule_button"
        android:layout_below="@+id/schedule_button"
        android:layout_marginTop="41dp"
        android:onClick="cancelAllJobs"
        android:text="Cancel all" />

</RelativeLayout>
